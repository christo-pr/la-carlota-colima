import styled from "styled-components"

/**
 * App Map styles
 * TODO: Pay the integration to bring this
 * back to life
 */
export const StyledMap = styled.div`
  width: 700px;
  height: 350px;
  border: 2px solid var(--blue);
  margin-top: 3rem;
  margin-right: auto;
  margin-left: auto;
}
`
