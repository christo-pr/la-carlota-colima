import styled from "styled-components"

/**
 * App footer
 */
export const AppFooter = styled.div`
  font-size: 0.8em;
  display: flex;
  justify-content: space-between;

  p {
    margin-bottom: 0;
  }
`
